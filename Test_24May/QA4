function getJsBasicsKeywordMeaning(term) {
 if (term === "NODE") {
    return "runtime";
  }
else if (term === " v8 ") {
    return "engine";
  }
  if (term === "npm") {
    return "package-manager";
  }
  if (term === "not-a-topic") {
    return "unknown";
  }}
