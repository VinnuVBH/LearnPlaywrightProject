let arr = [1, 2, 3, 4, 5];
console.log(arr.slice(1, 3)); // 2.3
// slice - {start, end-1(end index no. -1)}

console.log(arr.slice(2, 5)); // 3, 4, 5
console.log(arr.slice(2)); // everything from index 2


