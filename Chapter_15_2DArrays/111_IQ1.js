let s = [
    [1, 3, 5, 2]       // matrix size of this is     1 X 4
];

let grid = [
    [3, 4, 7],
    [5, 7, 1],
    [78, 95, 5]     // matrix size of this is 3 X 3 (n X n is called grid)
];

console.log(grid[0][0]);  // 3
console.log(grid[1][0]);// 5

console.log(grid.length); // row length (how many rows are there)
console.log(grid[0].length); // column length (how many columns are there)

